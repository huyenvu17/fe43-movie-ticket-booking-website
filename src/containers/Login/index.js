import React, { Component } from "react";
import FormLogin from "../Login/LoginForm/index";

export default class Login extends Component {
  render() {
    return (
      <div>
        <FormLogin />
      </div>
    );
  }
}
